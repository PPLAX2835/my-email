export default {
    EMAILS: [],
    CURRENT_EMAIL_INDEX: 0,
    setEmails(emails) {
        this.EMAILS = emails;
    },
    setCurrentEmailIndex(index) {
        this.CURRENT_EMAIL_INDEX = index;
    }
}
